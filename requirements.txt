# Core dependencies
anthropic>=0.18.0                       # Claude API for web searches

# Optional: For enhanced functionality
requests>=2.28.0                        # HTTP library for web requests

# Setup Wizard dependencies
flask>=3.0.0                            # Web framework for setup wizard
uszipcode>=1.0.1                        # US zip code database for location lookup
sqlalchemy-mate<2.0.0                   # Pin to 1.x for uszipcode compatibility

# Development (optional)
pytest>=7.0.0                           # Testing framework
black>=23.0.0                           # Code formatting
flake8>=6.0.0                           # Linting
